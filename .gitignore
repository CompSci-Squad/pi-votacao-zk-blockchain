# Dependencies
node_modules/

# Build artifacts (Hardhat + Foundry)
artifacts/
cache/
out/
broadcast/

# Foundry lockfile is generated; libs are submodules so don't track lib/ here
foundry.lock

# Environment variables (never commit secrets)
.env

# Coverage reports
coverage/
coverage.json

# TypeChain type bindings
typechain/
typechain-types/

# Hardhat ignition deployments (if used)
ignition/deployments/

# Misc
.DS_Store
*.log
.voting_addr
